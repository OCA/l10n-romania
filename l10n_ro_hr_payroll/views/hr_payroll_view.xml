<?xml version="1.0"?>
<odoo>
    <record id="view_hr_payslip_form" model="ir.ui.view">
        <field name="name">hr.payslip.form</field>
        <field name="model">hr.payslip</field>
        <field name="inherit_id" ref="hr_payroll.view_hr_payslip_form"/>
        <field name="arch" type="xml">
            <field name="worked_days_line_ids" position="replace">
                <field name="worked_days_line_ids">
                    <tree string="Worked Days" editable="bottom">
                        <field name="name"/>
                        <field name="code"/>
                        <field name="number_of_days" sum="Total Working Days"/>
                        <field name="number_of_hours"/>
                        <field name="contract_id"/>
                        <field name="sequence" invisible="True"/>
                        <field name="daily_base" invisible="True"/>
                        <field name="employer_days" invisible="True"/>
                        <field name="budget_days" invisible="True"/>
                        <field name="employer_amount" invisible="True"/>
                        <field name="total_amount" invisible="True"/>
                    </tree>
                    <form string="Worked Day">
                        <group col="4">
                        <field name="name"/>
                        <field name="code"/>
                        <field name="payslip_id"/>
                        <field name="sequence"/>
                        <field name="number_of_days"/>
                        <field name="number_of_hours"/>
                        <field name="contract_id"/>
                        </group>
                        <group col="4">
                        <field name="daily_base" invisible="True"/>
                        <field name="employer_days" invisible="True"/>
                        <field name="budget_days" invisible="True"/>
                        <field name="employer_amount" invisible="True"/>
                        <field name="total_amount" invisible="True"/>
                        </group>
                    </form>
                </field>
                <label for="working_days"/>
                <field name="working_days"/>
            </field>
        </field>
    </record>
</odoo>
